function [handles,hObject] = WheelCallback(Tag,handles,hObject)

% save('obj.mat','handles')

% flds = {'Normal','Abnormal'};
% Callback = {'WheelCallback'};
% % pos = [.25,0.015,.24,.015];
% pos = [.65,0.015 ,0.16,0.211];
% 
% s = funBuildo('wheel','spinner',pos,flds,...
%     [handles.score(handles.currentID),1-handles.score(handles.currentID)],'Callback',Callback);
% handles = funControl('wheel',handles,s);

end

